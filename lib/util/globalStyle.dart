import 'package:flutter/material.dart';

ThemeData themeData = ThemeData().copyWith(
    textTheme: const TextTheme().copyWith(
        bodyText1: const TextStyle(color: Colors.black, fontSize: 16)));

Color backgroundColor = const Color.fromRGBO(230, 239, 255, 1);
Color blueColor = const Color.fromRGBO(58, 126, 252, 1);
